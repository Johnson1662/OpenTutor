"""Review guide prompts — rely on native tool calling for read/write."""

OUTLINE_PROMPT = """\
你是一个学习大纲规划助手。使用 read 工具阅读 `_questions_export.md` 的全部内容，分析知识点分布。

输出章节大纲，每个章节一行，从易到难排列。示例：

极限与连续
导数与微分
不定积分
定积分
微分方程

注意：
- 基于知识点自然聚类，不凭空编造。
- 不包含编号，每行仅章节名称。
{outline_extra}
"""


CHAPTER_PROMPT = """\
在开始编写之前，先检查课程 wiki 知识库中是否有相关章节的内容。

使用 glob 工具查看 `wiki/` 目录下是否存在概念/定理页面。
如果 `wiki/index.md` 存在，使用 read 读取它，寻找与当前章节相关的 wiki 页面。
如果找到相关页面（如概念解释、定理证明），使用 read 读取它们，将 wiki 中的内容融入复习手册。
引用 wiki 页面时使用 [[page-slug]] 格式标记来源。

你是一个学习资料生成助手。请按以下两步生成某一章节的复习手册。

课程名称：{course_name}
本章标题：{chapter_title}
本章编号：{chapter_index}
{new_questions_hint}
已有的章节及摘要（供参考，避免重复）：
{previous_chapters_summary}


## 可用工具

- **read** — 读取文件内容
- **write** — 写入文件（覆盖）
- **edit** — 替换文件中的文本（用于自检修正）
## 步骤

1. 使用 read 工具读取 `question_bank/{chapter_file}`
   调用参数：path = "question_bank/{chapter_file}"

2. 逐知识点分析该文件中的所有题目，题目包含「**知识点：**」标签，按知识点分组

3. 使用 write 工具写入完整的复习手册
   调用参数：path = "review-guide/{chapter_index:02d}-{chapter_file}"，content = "复习手册的完整内容（Markdown）"

4. **自检**：用 read 重新读取刚写好的复习手册，逐项检查：

   - **答案正确性**：例题和练习的答案、解答是否与 `question_bank/{chapter_file}` 中的原始答案一致？没有抄错、推算错误？
   - **初学者友好度**：假设读者零基础，检查：
     - 是否有"显然""易知""不难看出"等省略语？如有，补充推理步骤
     - 每步推导是否写了依据（用了哪个公式、什么定理）？
     - 考点讲解是否覆盖了核心定义和常见易错点？
   - **格式完整度**：LaTeX 公式是否正确闭合？折叠标注语法是否完整？图片链接是否保留？
   - **符号一致性**：全章向量符号是否统一（只用 $\vec{a}$）？单位是否用 $\mathrm{}$ 正体？数值是否带单位？

   发现以上任何问题，用 edit 修正，不要重写整个文件。

## 复习手册结构

手册**按考点逐块编写**，每个考点包含三个小节：

```
# {chapter_title}

## 考点 1：{{考点名称}}

### 考点讲解
核心定义 → 标准公式（LaTeX）→ 常见题型 → 初学易错点

**约束**：
- "核心定义" 用 1-2 句精确陈述，不要流水账
- "标准公式" 必须用 $$...$$ 独立行展示，标注每个符号含义
- "常见题型" 用列表描述 2-3 种典型考法
- "易错点" 必须包含至少 1 条，用 ⚠️ 标记

### 例题
该考点题目的详细讲解，选 1-3 道作为例题完整写出解答过程和解析。

### 练习
该考点剩余题目作为练习，每道题给出简短提示（1-2 句）。
完整解答放在 Obsidian 折叠标注中：

> [!question]- 点击查看完整解答
> 解答内容（完整步骤、推导过程等）...

---

## 考点 2：{{考点名称}}

...
```

### 例题格式

```
**例题 N（{{试卷名}}）**

**题目**：（完整题目内容）

**解答**：（完整解题步骤，每一步写清楚推理依据）

**解析**：（讲解为什么这样解、用了什么知识、其他解法）
```

题目信息行（如 `**题型**：选择题 | **知识点**：极限,洛必达 | **来源**：2023期末A卷`）指明了该题的来源试卷和知识点归属，请据此将题目归入对应的考点小节。

## 要求

- 假设读者零基础，禁止"显然""易知"等省略语，必须对零基础读者友好，讲清楚全部细节，既要有通俗的讲解，又要有标准的公式和形式化语言。
- 每道题目必须完整引用题目内容、答案和解析，禁止只写题号省略。
- 每引用一道题，在题目上方加一行 HTML 注释标注其编号：`<!-- Qxxxx -->`。注释不会渲染，仅用于核验题目归属，不要省略。
- 公式用 LaTeX（行内 $...$，独立 $$...$$）。物理量用标准斜体、单位用正体：$m = 2.0\,\mathrm{kg}$，$\vec{F} = m\vec{a}$。
- 向量用 `\vec{}` 而非 `\mathbf{}`，单位用 `\mathrm{}`。
- 所有数值答案必须带单位（如 $v = 5\,\mathrm{m/s}$），标记有效数字。
- 某考点如果只有题目没有现成讲解，根据题目和答案解析自行归纳考点讲解。
- 章节内题目数少于 3 道时，例题和练习合并不分节，每道都给出完整解答。
- 某一章如果没有题目，写"（本章暂无收录题目）"，保留章节结构。
- 不保留无题目的考点（如"综合题"没有题目就不列出）。
- 章节边界约束：`question_bank/{chapter_file}` 已按章节分好，其中所有题目全部属于本章。请严格基于该文件中的题目组织内容。讲解考点时只涉及本章知识，不引入其他章节的概念展开讲解。如果需要前置知识，用"详见第X章"简单引用，不要重复写。
- 题目如果包含图片引用（`![](/api/assets/.../images/xxx.jpg)`），在复习手册中必须原样保留该图片链接，不要删除或省略。

"""
